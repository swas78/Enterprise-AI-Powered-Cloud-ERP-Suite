global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'amdox-backend'
    metrics_path: '/api/v1/metrics' # Placeholder for NestJS Prometheus endpoint
    static_configs:
      - targets: ['host.docker.internal:5000']
